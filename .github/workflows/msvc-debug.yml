name: Build MSVC Profile (Manual)

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build configuration'
        required: true
        default: 'Debug'
        type: choice
        options:
          - Debug
          - Release

jobs:
  windows-msvc-profile:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - uses: ilammy/msvc-dev-cmd@v1

      - name: Cache FetchContent
        uses: actions/cache@v4
        with:
          path: |
            _deps-cache
            third-party/raylib
            third-party/tracy
          key: deps-windows-msvc-profile-${{ hashFiles('CMakeLists.txt') }}

      - run: ./build.bat msvc ${{ inputs.build_type }} static profile

      - uses: actions/upload-artifact@v4
        with:
          name: windows-msvc-profile-${{ inputs.build_type }}
          path: |
            _build/msvc-${{ inputs.build_type }}/${{ inputs.build_type }}/app.exe
            _build/msvc-${{ inputs.build_type }}/${{ inputs.build_type }}/app.pdb
            _build/msvc-${{ inputs.build_type }}/${{ inputs.build_type }}/assets/
          retention-days: 3